/target
*.uf2
*.elf
# Cargo.lock IS committed — this is an application/binary crate, so pin the exact
# dependency versions for reproducible builds.

# Internal dev checkpoint — kept locally, not published
/RESUME.md
